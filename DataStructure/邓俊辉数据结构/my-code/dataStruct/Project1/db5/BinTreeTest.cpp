#include <iostream>
#include "../db5/myBinTree.hpp"
/*
int main() {
	myBinTree<int> t = myBinTree<int>();
	int x = 11;
	BinNodePosi(int) root = t.insertAsRoot(x);
	BinNodePosi(int) l1 = t.insertAsLC(root, 9);
	BinNodePosi(int) r1 = t.insertAsRC(root, 15);
	BinNodePosi(int) l2 = t.insertAsLC(l1, 7);
	BinNodePosi(int) r2 = t.insertAsRC(r1, 20);

	BinNodePosi(int) l1r = t.insertAsRC(l1, 10);
	//11
//		9				15
//7		10				20

	//t.travPre(visit<int>);
	//t.travIn(visit<int>);

	//succ的测试
	//cout << "l1 的下一位置" << l1->succ()->data << endl;
	//cout << "l1r 的下一位置" << l1r->succ()->data << endl;
	//cout << "l2 的下一位置" << l2->succ()->data << endl;

	//t.travPost(visit<int>);

	t.travLevel(visit<int>);

	t.remove(l1r);
	return 0;
}
*/